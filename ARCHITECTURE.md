# 🏗️ Extension Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Instagram Reels Page                         │
│                  (instagram.com/reels/*)                        │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ Content Script Injected
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                      CONTENT.JS                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Floating UI Components:                               │    │
│  │  • Purple Camera Button (Capture)                      │    │
│  │  • Pink Download Button (Export CSV)                   │    │
│  │  • Counter Badge (Total Logs)                          │    │
│  │  • Notification System                                 │    │
│  └────────────────────────────────────────────────────────┘    │
│                          │                                      │
│                          │ User Clicks Button                   │
│                          ↓                                      │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Event Handlers:                                       │    │
│  │  • handleCapture() - Sends message to background       │    │
│  │  • handleDownload() - Triggers CSV export              │    │
│  │  • updateCounter() - Refreshes badge count             │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ chrome.runtime.sendMessage()
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                     BACKGROUND.JS                               │
│                  (Service Worker)                               │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Message Listeners:                                    │    │
│  │  • 'captureScreenshot' → handleCaptureScreenshot()     │    │
│  │  • 'downloadCSV' → handleDownloadCSV()                 │    │
│  └────────────────────────────────────────────────────────┘    │
│                          │                                      │
│                          ↓                                      │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Core Functions:                                       │    │
│  │  1. chrome.tabs.captureVisibleTab() → PNG Data URL    │    │
│  │  2. Collect URL + Timestamp                            │    │
│  │  3. Create entry: {image, url, timestamp}              │    │
│  │  4. Save to Chrome Storage                             │    │
│  │  5. Generate CSV from all entries                      │    │
│  │  6. chrome.downloads.download() → Save CSV             │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ chrome.storage.local API
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                   CHROME STORAGE                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Storage Schema:                                       │    │
│  │  {                                                     │    │
│  │    "reelsLog": [                                       │    │
│  │      {                                                 │    │
│  │        "image": "data:image/png;base64,...",          │    │
│  │        "url": "https://instagram.com/reels/xyz",      │    │
│  │        "timestamp": "2026-02-18 00:15:30"             │    │
│  │      },                                                │    │
│  │      ...                                               │    │
│  │    ]                                                   │    │
│  │  }                                                     │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  📊 Features:                                                   │
│  • Unlimited storage (permission granted)                      │
│  • Persists across browser restarts                            │
│  • Accessible from all extension contexts                      │
└─────────────────────────────────────────────────────────────────┘
                          │
                          │ Data Retrieved
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                    POPUP.HTML + POPUP.JS                        │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Extension Popup Interface:                            │    │
│  │  • Display total logs count                            │    │
│  │  • Show storage usage (MB)                             │    │
│  │  • Export CSV button                                   │    │
│  │  • View logs in gallery                                │    │
│  │  • Clear all logs (with confirmation)                  │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                          │
                          │ CSV Export
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                    CSV FILE OUTPUT                              │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  insta_logger_2026-02-18.csv                           │    │
│  │  ────────────────────────────────────────              │    │
│  │  Image Data URL,Share Link,Date-Time                   │    │
│  │  "data:image/png;...",https://...,2026-02-18 00:15:30 │    │
│  │  "data:image/png;...",https://...,2026-02-18 00:20:45 │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

### Capture Flow
```
User Action → Content Script → Background Worker → Chrome Storage → UI Update
    ↓              ↓                  ↓                   ↓              ↓
 Click Btn    Send Message      Capture Screen      Save Data    Update Badge
              + URL/Time         → PNG Data URL      in Array     Show Success
```

### Export Flow
```
User Action → Content Script → Background Worker → Storage Retrieval → CSV Generation
    ↓              ↓                  ↓                    ↓               ↓
 Click Btn    Send Message      Get All Logs        Read Array      Format CSV
                                                                    → Download
```

---

## 🔐 Permissions Map

| Permission        | Used By        | Purpose                        |
|-------------------|----------------|--------------------------------|
| storage           | All contexts   | Save/retrieve log data         |
| unlimitedStorage  | Background     | Store large image data         |
| downloads         | Background     | Export CSV files               |
| activeTab         | Background     | Capture visible tab            |
| scripting         | Manifest       | Inject content scripts         |
| host_permissions  | Content Script | Access Instagram Reels pages   |

---

## 🎯 Component Responsibilities

### Content Script (`content.js`)
- ✅ Detect Instagram Reels pages
- ✅ Inject floating UI buttons
- ✅ Handle user interactions
- ✅ Display notifications
- ✅ Update counter badge
- ✅ Send messages to background

### Background Worker (`background.js`)
- ✅ Listen for messages
- ✅ Capture screenshots
- ✅ Store data persistently
- ✅ Generate CSV files
- ✅ Trigger downloads
- ✅ Handle errors

### Popup (`popup.html` + `popup.js`)
- ✅ Display statistics
- ✅ Manage stored data
- ✅ Export functionality
- ✅ View logs interface
- ✅ Clear data option

### Styles (`style.css`)
- ✅ Modern gradient buttons
- ✅ Smooth animations
- ✅ Notification styling
- ✅ Responsive design
- ✅ Dark mode support

---

## 🚀 Extension Lifecycle

1. **Installation**
   ```
   Extension Installed → Initialize Storage → Set Empty Array
   ```

2. **Page Load**
   ```
   Instagram Reels Detected → Inject Content Script → Show UI
   ```

3. **Capture**
   ```
   Button Click → Send Message → Take Screenshot → Save Data → Update UI
   ```

4. **Export**
   ```
   Export Click → Retrieve Data → Generate CSV → Download File
   ```

5. **Management**
   ```
   Popup Opened → Load Stats → Display Info → Handle Actions
   ```

---

**Architecture**: Clean, modular, and follows Chrome Extension best practices! 🎉
